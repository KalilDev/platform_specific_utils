import 'package:cached_network_image/cached_network_image.dart';
import 'package:flutter/painting.dart' show AssetImage;
//getImageProvider(String url, {Map<String, String> headers}) => (url?.startsWith('asset') ?? false) ? AssetImage(url) : CachedNetworkImageProvider(url,headers: headers);